#pragma once
#include <vector>

extern std::vector<std::vector<int> > dummyMap;

// s—ñ‚ğ“]’u‚·‚é
std::vector<std::vector<int> > transporseMatrix(
    const std::vector<std::vector<int> > &matrix);
